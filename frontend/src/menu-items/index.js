import unauthorized from './unauthorized';
import authorized from './authorized';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [unauthorized, authorized]
};

export default menuItems;
